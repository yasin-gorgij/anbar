<h1>New Item</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.item_path(@conn, :create, @inventory_id)) %>

<span><%= link "Back", to: Routes.inventory_path(@conn, :show, @inventory_id) %></span>
